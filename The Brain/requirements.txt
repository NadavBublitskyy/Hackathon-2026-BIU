# This file lists the Python packages required by The Brain service.

# FastAPI provides the web API framework.
fastapi==0.115.6
# HTTPX provides the async HTTP client for OpenRouter requests.
httpx==0.28.1
# Pydantic Settings loads typed configuration from environment variables and .env.
pydantic-settings==2.7.1
# eval-type-backport lets Pydantic evaluate Python 3.10 union annotations under Python 3.9.
eval-type-backport==0.3.1
# Python Multipart lets FastAPI receive form fields and uploaded JSON files.
python-multipart==0.0.20
# Uvicorn runs the FastAPI application inside the container.
uvicorn[standard]==0.34.0
# ChromaDB stores and retrieves Memory code chunk embeddings locally.
chromadb
# PyGithub powers the Milestone 1 public GitHub ingestor.
PyGithub==2.5.0
# python-dotenv lets the Milestone 1 ingestor read an optional GitHub token.
python-dotenv==1.0.1
# NetworkX powers the Milestone 3 graph builder.
networkx==3.4.2
# tree_sitter_languages provides parsers for the code chunker.
tree_sitter_languages==1.10.2
